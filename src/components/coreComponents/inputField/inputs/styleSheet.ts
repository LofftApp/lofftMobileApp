import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  inputContainer: {
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
  },
  inputFieldTextStyle: {
    margin: 0,
    marginLeft: 11,
    paddingVertical: 8,
    flex: 1,
  },
  dropDownContainer: {
    borderWidth: 2,
    padding: 0,
    margin: 0,
  },
});
