import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const ChatCard = ({name}) => {
  return(
    <View>
      <Text>I am just a chat card</Text>
    </View>
  );
};

const styles = StyleSheet.create({

});

export default ChatCard;
